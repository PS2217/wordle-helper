import React from 'react';

import './Suggestion.css';

const Suggestion = props => {
    return (
        <React.Fragment>
            
        </React.Fragment>
    );
};

export default Suggestion;